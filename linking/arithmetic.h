// This headeerfile contains the (DECLARATIONS ONLY) of the functions, not the implementation

// Include the preprocessor directices (as macros) to avoid the 'already assigned' problem in the compilation
#ifndef ARITHMETIC
#define ARITHMETIC

int Sum(int a, int b);

int Sub(int a, int b);

int Mult(int a, int b);

float Div(int a, int b);


#endif // end the if statemenet block for the preprocessor directive
