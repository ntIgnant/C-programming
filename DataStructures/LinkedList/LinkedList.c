#include <stdio.h>
#include <stdlib.h>
#include <string.h>


typedef struct Node{
	int data; // Data here will be a number
	struct *Node next; // Pointer to the 'next node'
}Node;

int main(){
	
	
	
	return(0);
}
