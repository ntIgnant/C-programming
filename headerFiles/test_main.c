#include <arithmetic_utils.h> // This way of including the headerfile will not work because
                              // C checks for the headerfiles inside the path "/usr/include"
                              //
                              // To Include the header file as .h, it needs to be in between double
                              // quotation marks, so like
                              //
                              // #include "arithmetic_utils.h" | this would work
